import React from 'react';
import './spinner.css';
//import MDSpinner from "react-md-spinner"; // попробовать

const Spinner = () => {
	return (
    <div className="spinner">
      <div className="spinner_inner"></div>
    </div>
  );
}

export default Spinner;
